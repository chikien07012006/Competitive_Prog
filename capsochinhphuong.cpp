#include<bits/stdc++.h>
using namespace std;
#define ll long long
#define For(i,a,b) for(ll i=a;i<=b;i++)
const ll mod=998244353;
signed main()
{
    ios_base::sync_with_stdio(0);
    ll n; cin>>n;
    For(i,1,n)
    {
        ll l=0,r=i-1,mid,tam=1e18;
        while(l<=r)
        {
            mid=(l+r)/2;
        }
    }
}

