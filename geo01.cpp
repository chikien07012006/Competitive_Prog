#include<bits/stdc++.h>
using namespace std;
#define ll long long
#define For(i,a,b) for(ll i=a;i<=b;i++)
signed main()
{
    ios_base::sync_with_stdio(0);

    double n; cin>>n;
    double h=sqrt(2);
    double t=double(n)*(double(4)+double(2)*h);
    cout << std::setprecision(10);
    cout<<t;
}




