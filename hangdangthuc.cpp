#include<bits/stdc++.h>
using namespace std;
#define ll long long
#define For(i,a,b) for(ll i=a;i<=b;i++)
ll d[100005];
int main()
{
    ios_base::sync_with_stdio(0);
    ll n,res=0; cin>>n; ll a[n+5];
    For(i,1,n) {cin>>a[i];d[a[i]]=d[a[i]]+1;}
}
