#include<bits/stdc++.h>
using namespace std;
int main()
{
    freopen("NENXAU.INP","w" ,stdin);
    freopen("NENXAU.OUT","w" ,stdout);
    string s;
    cin>>s;
    long long d=1;
    for(int i=0;i<=s.size()-1;i++)
    {
        if (s[i] !=s[i-1]);
        {
            cout<<d;
            c=s[i];
            d=1
        }else d++;
    }
}
