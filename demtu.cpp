#include<bits/stdc++.h>
using namespace std;
typedef long long ll;
int main()
{
    ios_base::sync_with_stdio(0);
    int n;string s;
    cin>>n;
    for(int i=1;i<=n;i++)
    {
        getline(cin,s);
        ll d=0,t=0,j=0,res=0;
        for(ll i=0;i<s.size();i++)
        {
            if('a'<=s[i]&&s[i]<='z')
            {
                d++;
            }
            else
            {
                if(t==d)
            }
        }
    }
}
