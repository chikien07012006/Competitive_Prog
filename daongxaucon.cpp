#include<bits/stdc++.h>
using namespace std;
int main()
{
    ios_base::sync_with_stdio(0);
    string n;cin>>n;long long t=n.size()-1;long long q;cin>>q;long long a[q];
    for(long long i=1;i<=q;i++)
    {
        cin>>a[i];a[i]--;
        for(long long j=a[i];j<=((t-a[i])-a[i])/2;j++)
        {
            for(long long k=t-a[i];k>=)
        }
    }
}
