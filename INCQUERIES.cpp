#include<bits/stdc++.h>
using namespace std;
#define ll long long
#define For(i,a,b) for(ll i=a;i<=b;i++)
signed main()
{
    ios_base::sync_with_stdio(0);
    ll n,k,tam=0; cin>>n>>k; ll a[n+5];
    For(i,1,n) cin>>a[i];
    while(k--) 
    {
        ll l,r; cin>>l>>r;
        
    }
}