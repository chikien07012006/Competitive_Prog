#include<bits/stdc++.h>
using namespace std;
int main()
{
    long long n,t,max=0;
    cin>>n;
    long long a[n];
    for(int i=0;i<n;i++)
    {
        cin>>a[i];
    }
    for(int i=0;i<n;i++)
    {
        t=a[i];
        if(t>max)
        {
            max=t;
        }
        for(int j=i+)
    }
}
