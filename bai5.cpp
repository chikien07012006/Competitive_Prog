#include<bits/stdc++.h>
#include<math.h>
using namespace std;
void xl(long long a,long long n)
{
    cout<<pow(a,n);
}
int main()
{
    ios_base::sync_with_stdio(0);
    long long a,n;
    cin>>a>>n;
    xl(a,n);
}
