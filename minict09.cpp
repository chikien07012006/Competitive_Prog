#include<bits/stdc++.h>
using namespace std;
#define ll long long
int main()
{
    ios_base::sync_with_stdio(0);
    ll n,k; cin>>n>>k; ll a[n+5],w[k+5];
    for(ll i=1;i<=n;i++) cin>>a[i];
    for(ll i=1;i<=k;i++) cin>>w[i];
    sort(w+1,w+1+k);
    sort(a+1,a+1+n,greater<long long>());
    for(ll i=1;i<=k;i++)
    {
        w[i]
    }
}
