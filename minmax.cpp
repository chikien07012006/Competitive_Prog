#include<stdio.h>
int main()
{
    long long x, min=5000000000, max=0, s=0;
    for(int i=1;1<=i<=5;i++);
    scanf("%d",&x);
    {
       s=s+x;
       if(x<min) min=x;
       if(x>max) max=x;
    }
    printf("%d",min);
    printf("%d",max);
}
